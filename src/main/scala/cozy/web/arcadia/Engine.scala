package cozy.web.jetty

/*
 * @since   Jan. 23, 2022
 * @version Jan. 23, 2022
 * @author  ASAMI, Tomoharu
 */
class Engine() extends {
  // val r = new WebEngine(webPlatformContext, app, extend, _web_config(app))
}
