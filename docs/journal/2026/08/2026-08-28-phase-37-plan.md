# Phase 37 P37-01B-REVIEW-FIX-001 Step-review Repair Journal

Date: 2026-08-28

Status: authorized documentation repair pending parent validation and
re-review

## Frozen repair identity and boundary

The parent supplied one complete frozen Fix Manifest for
`P37-01B-REVIEW-FIX-001` at `Phase 37 / P37-01 / P37-01B`. It authorizes only
the three Step-review findings below within the single Phase 37 boundary and
does not create a child phase. Phase 37 and `LOGIC37-01` remain IN PROGRESS;
every Phase 37 checklist item and box remains open.

The stable finding IDs used for this repair are:

- `P37-01B-REVIEW-FIX-001-F1`: Composition `validate`, `inspect`, and `convert`
  must require explicitly named `--presentation-catalog <file>` in addition
  to `--explanation-catalog <file>` and applicable direct source/asset
  bindings, so an authored P36 Logical Pattern/Relation graph is never parsed
  or validated without its direct P36 catalog.
- `P37-01B-REVIEW-FIX-001-F2`: explanation-catalog canonical ordering must be
  explicit and deterministic while semantic authored arrays remain unchanged.
- `P37-01B-REVIEW-FIX-001-F3`: Phase 37 ledger and strategy wording must define
  `expand` as deterministic validation, normalization, and copying of
  explicitly authored steps and typed graphs, preserving product and
  problem-solution roles/Relations rather than generating or inferring them.

## Finding-to-change mapping

- `P37-01B-REVIEW-FIX-001-F1` was repaired in
  `docs/design/explanation-composition.md` and
  `docs/spec/explanation-composition.md` by binding Composition parsing and
  validation to both named catalogs and updating all three Composition rows
  in the direct-file command/input matrix.
- `P37-01B-REVIEW-FIX-001-F2` was repaired in the design and specification by
  defining ascending `(id,version)` ordering for both pattern arrays,
  ascending `roles.order`, name ordering for fact/parameter definitions,
  `(id,version)` ordering for compatible subjects, and the fixed reserved
  Narrative / Argument order after strict duplicate validation. Semantic
  authored arrays remain in authored or contract-defined order.
- `P37-01B-REVIEW-FIX-001-F3` was repaired in
  `docs/phase/phase-37.md`, `docs/phase/phase-37-checklist.md`,
  `docs/strategy/cozy-development-strategy.md`, and the design/specification
  contracts by removing graph-generation implications, defining the bounded
  meaning of `expand`, and requiring authored typed roles/Relations to be
  retained for `product-overview`, `product-mechanism`, and
  `problem-solution`.

## Exact owned paths

The sole allowed repository is Cozy at `/Users/asami/src/dev2025/cozy`. The
exact owned paths for this Fix Manifest are:

- `docs/design/explanation-composition.md`;
- `docs/spec/explanation-composition.md`;
- `docs/phase/phase-37.md`;
- `docs/phase/phase-37-checklist.md`;
- `docs/strategy/cozy-development-strategy.md`; and
- `docs/journal/2026/08/2026-08-28-phase-37-plan.md`.

No other documentation path is in this repair boundary. In particular,
`docs/phase/README.md`, P30/P36 authority documents, Scala, tests, external
repositories, and user-dirty archive paths are excluded. Existing edits in
the owned Phase 37, checklist, and strategy files were preserved while
applying only these three findings.

This is an authorized documentation repair only. It does not authorize
implementation, validation, review, re-review, staging, commit, publication,
deployment, renderer or runtime execution, external repository mutation, or
downstream consumer acceptance.

## Preserved unrelated dirty paths

The following user/other-agent changes were dirty when this repair began and
were preserved without edit:

- `docs/phase/README.md`;
- `src/main/scala/cozy/archive/CozyCarPublisher.scala`;
- `src/main/scala/cozy/archive/CozySarPublisher.scala`;
- `src/main/scala/cozy/archive/RepositoryArtifactPublisher.scala`;
- `src/test/scala/cozy/CozyCarPublisherSpec.scala`; and
- `src/test/scala/cozy/CozySarPublisherSpec.scala`.

No archive path, source path, test path, P30/P36 authority, or external
repository was added to the P37-01B-REVIEW-FIX-001 mutation boundary.

## Modified-Scala and executable-specification compliance ledger

- Scala target-program paths modified by P37-01B-REVIEW-FIX-001: none.
- Executable Specification paths modified by P37-01B-REVIEW-FIX-001: none;
  executable specifications remain a later P37 implementation-slice
  responsibility.
- Naming review for newly authored Scala declarations: not applicable.
- Existing dirty Scala and test paths listed above: preserved and not
  attributed to this repair.

## Hygiene, Development Candidate, and existing debt boundary

The P37-01B-REVIEW-FIX-001 Hygiene ledger is empty: no new `HYG-*` record is
admitted by this documentation repair. The Development Candidate ledger is
also empty: no new `DEV-*` record is created or deferred here. Existing
strategy item `DEV-007` remains the prior Phase 37 direction record and is not
a new repair finding.

### Existing Debt (Separate Follow-up)

No existing debt was edited or promoted by this repair. Any later
implementation, Executable Specification, validation, or broader review work
remains separately owned and separately gated.

## Evidence and stop boundary

The documentation edits satisfy the frozen finding-to-change requirements;
overall manifest acceptance remains pending the parent's read-only diff
verification, focused validation, and re-review. No validation, review,
re-review, or commit receipt is recorded by this journal entry. The parent
owns those evidence and transition decisions. No SBT, Cozy/Dox, launcher,
server, runtime, network, publication, deployment, staging, or commit
operation was run or claimed by P37-01B-REVIEW-FIX-001.

## P37-01 Step-closure evidence addendum

The parent-provided static preflight evidence is clean: the P37-01
documentation diff stayed within its frozen owned paths and `git diff --check`
reported no whitespace errors. This evidence was not rerun by the closure
bookkeeping worker.

The initial P37-01 lightweight review admitted findings
`P37-01B-REVIEW-FIX-001-F1`, `P37-01B-REVIEW-FIX-001-F2`, and
`P37-01B-REVIEW-FIX-001-F3`. The complete focused re-review returned
`FOCUSED_PASS` and closed all three findings. On that evidence, the P37-01
design/specification contract is review-clean and `LOGIC37-01` is COMPLETE;
the local acceptance commit remains pending and has no recorded commit hash.

Phase 37 remains IN PROGRESS, with `LOGIC37-02` through `LOGIC37-06`
NOT STARTED/open. Phase 37 full validation and Phase 37 full review were not
run or claimed, and no publication, push, downstream consumer acceptance, or
Phase closure is claimed. Implementation and executable specifications remain
P37-02 onward. This addendum records Step closure bookkeeping only; no tests,
SBT, validation, review, re-review, staging, commit, runtime, publication, or
deployment operation was run by this worker.
