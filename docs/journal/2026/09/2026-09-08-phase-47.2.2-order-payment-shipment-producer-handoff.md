# Phase 47.2.2 Order/Payment/Shipment Producer Handoff

Handoff ID: `P4722-01A-PRODUCER-HANDOFF-001`

Date: 2026-09-08

Status: Cozy producer-fixture/handoff boundary prepared; CNCF consumer
acceptance pending

Source repository: `asami/cozy`

Target consumer: `asami/goldenport-cncf` Phase 64.2, after the Phase 64.1
prerequisite

## Exact producer inputs and generated outputs

Checked-in source fixture:

```text
src/test/resources/modeler/order-payment-shipment-action-program.cml
```

Executable specification:

```text
src/test/scala/cozy/modeler/OrderPaymentShipmentActionProgramSpec.scala
```

The real `modeler-scala` route emits the following producer sources for the
normalized definition:

```text
target/scala-3.3.8/src_managed/main/scala/domain/composite/statemachine/actionprogram/
  CompositeStateMachineActionProgramAbi.scala
  LogicalActionCompiler.scala
  CompositeStateMachineActionProgramBootstrap.scala
  OrderPaymentShipmentActionProgram1.scala
```

The canonical descriptor-only sidecar is:

```text
target/cozy/cml-logical-action-program.json
```

The additive ABI version is exactly `cozy.cml.logical-action-program.v1`.
The existing CSM v1, projection, and producer-metadata ABI surfaces remain
unchanged.

## Producer evidence

The fixture has typed `payment` and `shipment` subjects and complete
`Awaiting`/`Authorized` by `Waiting`/`Reserved` derivations. It starts at
`payment.Awaiting, shipment.Waiting`.

`record-authorization` resolves to
`PaymentService.recordAuthorization` with `LOCAL`, `REQUIRED`, and required
idempotency metadata. Its constituent occurrences are authored at payment
exit, transition, and entry. `reserve-shipment` resolves to
`ShipmentService.reserveShipment` with `EXTERNAL`, `OUTSIDE_UNIT_OF_WORK`,
required idempotency, and compensation-handler reference `release-shipment`.
The existing metadata-bearing `release-shipment` Action resolves to
`ShipmentService.releaseShipment`.

The executable specification generates the checked-in fixture through the real
route twice, compares the generated Scala and JSON bytes, proves lower payment
occurrence order before the derived `AwaitingPayment` to `AwaitingShipment`
reservation, and checks provenance and metadata retention. ScalaCheck covers
repeated canonical descriptor rendering.

## Consumer binding-only contract

The generated compiler accepts only consumer-supplied existing
`org.goldenport.cncf.unitofwork.ExecProgram[Unit]` bindings. It composes those
bindings in generated causal order and retains descriptors; it does not
serialize executable programs or choose runtime behavior. CNCF owns admission
of the generated source and all runtime interpretation under its existing
`ExecProgram[UnitOfWorkOp, A]` path.

## External ownership and prohibited Cozy work

CNCF Phase 64.1 is a prerequisite for Phase 64.2. CNCF Phase 64.2 owns UTP-02
ABI admission plus UTP-06 through UTP-09: production identity, simple/local
StateMachine outcomes, composite/Workflow execution, compensation, duplicate
handling, recovery, idempotency acceptance, and any generic
`UnitOfWorkOp`-extension decision. These items are external-pending and are
not current Cozy completion claims.

Cozy must not compile or execute the consumer source, add transaction,
compensation, retry, recovery, or `UnitOfWorkOp` behavior, run CNCF runtime or
consumer tests, mutate the CNCF repository, or claim Phase 64.1/64.2
acceptance from this producer handoff.
