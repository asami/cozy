# Phase 46.1 Hygiene Follow-up

Date: 2026-09-05

## Accepted record

- `HYG-46.1-001`: pre-existing private `Fixture` field and parameter naming
  debt in `src/test/scala/cozy/document/CozyDocumentPresentationSemanticsSpec.scala`
  (the existing declarations around lines 204–211 and 220).
- `HYG-46.1-002`: stale Phase 46.1 strategy summary in
  `docs/strategy/cozy-development-strategy.md` (the existing Phase section and
  DEV-017/current-priority entries around lines 1558–1581 and 1687–1742).
- `HYG-46.1-003`: pre-existing Scala compiler warning from the nested private
  `Fixture` case class at line 224 of
  `src/test/scala/cozy/document/CozyDocumentPresentationSemanticsSpec.scala`:
  the outer reference cannot be checked at run time.

## Discovery and evidence

- Discovery state/date: discovered on 2026-09-05 during the prior Phase 46.1
  `BASELINE_REVIEW`.
- Repository: `/Users/asami/src/dev2025/cozy`.
- Repository-relative location: `src/test/scala/cozy/document/CozyDocumentPresentationSemanticsSpec.scala`,
  private `Fixture` fields at lines 204–211 and private `_fixture` parameters at
  line 220.
- Symbols: `root`, `coreBytes`, `coreValue`, `semantics`, `composition`,
  `catalogs`, `bindings`, and `_fixture(coreText, sourceText, assetText)`.
- Evidence: these pre-existing private internal `Fixture` fields and private
  method parameters require flatcase under repository rules; their current
  names are nonconforming.
- Category/risk: naming-only hygiene debt; nonblocking and semantically inert.

## Disposition

This is naming-only, nonblocking hygiene debt. It predates the
`PROJECT46-01A-cross-media-projection` implementation Slice and remains
outside this Slice. No fixture or private naming was changed while adding the
Structure-level projection coverage.

Why outside the frozen Phase: this Slice owns defensive cross-media projection
coverage only; renaming the existing fixture would broaden the frozen scope.

Hygiene status: OPEN

Proposed hygiene grouping: a separately authorized Cozy naming-only hygiene
batch. The follow-up must preserve the cross-media executable behavior and must
not widen Phase 46.1 scope.

## Additional review record

`HYG-46.1-002` was discovered by the independent `PROJECT46-01` Step review
on 2026-09-05. The strategy still labeled Phase 46.1 as planned and included
Article 8 local acceptance, which conflicted with the active Phase status and
the `P461-DEC-ROOT-001` exclusion. This was management-document freshness
debt and did not alter accepted cross-media behavior or invalidate focused test
evidence. It is resolved by the distinct Phase 46.1 release-closure strategy
projection; this does not admit separately-owned Article 8 driver work.
`HYG-46.1-001` and `HYG-46.1-003` remain OPEN.

## Validation warning record

`HYG-46.1-003` was observed by focused validation
`P461-PROJECT46-01-STEPVAL-002` on 2026-09-05. It is a pre-existing
test-structure compiler warning and does not affect the successful eight-test
result, the accepted cross-media behavior, or the frozen Slice scope. Resolve
it only in a separately authorized Cozy test-structure hygiene batch.

## 2026-09-05 location correction

The later `PROJECT46-02A-confirmation-html` executable scenarios moved the
existing Fixture declarations without changing their provenance or category.
The current private `Fixture` fields are around lines 278–285, `_fixture`
parameters are around line 294, and the compiler warning is at the nested
`Fixture` declaration around line 278. `HYG-46.1-001` and `HYG-46.1-003`
remain pre-existing, nonblocking hygiene; this correction neither admits their
repair nor changes the Phase boundary.
