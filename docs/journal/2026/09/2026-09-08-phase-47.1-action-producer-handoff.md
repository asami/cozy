# Phase 47.1 Action Producer Handoff

Handoff ID: `P47.1-ACTX06-PRODUCER-HANDOFF-001`

Date: 2026-09-08

Status: completed Cozy producer-side handoff record

Source repository: `asami/cozy`

Target consumer: `asami/goldenport-cncf` Phase 64.1

## Handoff boundary

This record completes Cozy's producer-side recording of the accepted CML
Action metadata, static validation, and deterministic producer-generation
evidence. It asks `goldenport-cncf` Phase 64.1 to evaluate and accept the
runtime contract in its own scope. It is not an external-repository write,
delivery acknowledgement, consumer acceptance result, or claim that Phase
64.1 work has started or completed.

The accepted producer authorities are:

- [CML Action Producer Metadata](../../../design/cml-action-producer-metadata.md);
- [CML Action Compensation Handler
  Binding](../../../design/cml-action-compensation-handler-binding.md); and
- [CML Action Producer Metadata
  Generation](../../../design/cml-action-producer-generation.md).

## Cozy evidence chain

The producer contract is implemented by this ordered Phase 47.1 chain:

```text
557166d5a0a00ba510757e61ace03f77686cee22
  -> 989667f96f6cf796ec339329f980cc07c92d4590
  -> 087c11bf951407e783db2036fcdfdb606786b9c0
  -> 124e79159cc15b6d5a53d1a7b991104be87d0b9a
  -> 180dacc6db7c51cc38b86eb176ac6684e2b3e281
```

The implemented producer schema is exactly:

```text
cozy.cml.action-producer-metadata.v1
```

Both public Scala generation routes emit additive producer sources at:

```text
target/scala-3.3.8/src_managed/main/scala/domain/composite/statemachine/actionproducer/
  CompositeStateMachineActionProducerMetadataAbi.scala
  CompositeStateMachineActionProducerMetadataBootstrap.scala
  <definition>ActionProducerMetadata<n>.scala
```

They also emit the canonical JSON sidecar:

```text
target/cozy/cml-action-producer-metadata.json
```

The parser accepts and normalizes the direct Action fields `EFFECT`,
`TRANSACTION`, `IDEMPOTENCY`, `IDEMPOTENCY-KEY`, and
`COMPENSATION-HANDLER`. It requires the base metadata as a complete set,
enforces the exact enum and idempotency-key pairing, rejects empty or duplicate
authored metadata, and allows a compensation handler only for `EXTERNAL` plus
`OUTSIDE_UNIT_OF_WORK`. Compensation handler references remain opaque stable
application references: Cozy neither resolves nor executes a handler.

The additive output preserves the existing CSM v1 ABI and bootstrap, generated
definitions, logical Action identity, Action-occurrence provenance, and CSM
projection. Actions without normalized Phase 47.1 metadata produce no producer
metadata record.

Focused executable validation is recorded as
`P47.1-ACTX05-TEST-002`, invocation `79991-20260907T204310Z`: 3 suites,
21 passed, 0 failed, with the serialized SBT lock released.

## Consumer request and exclusions

Phase 64.1 alone is asked to evaluate and accept the runtime contract from its
own repository and authority. This handoff explicitly excludes Cozy runtime
acceptance and all UnitOfWork execution or rollback, 2PC provider, capability,
or admission behavior, handler resolution or execution, Saga, retry, or
recovery behavior, durable `RecoveryRequired`, and manual administrative
recovery.

Phase 47.2 is outside this handoff. No external repository is modified, and no
push, publication, or deployment is performed or implied by this record.
